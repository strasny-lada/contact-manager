{% extends 'base.html.twig' %}

{% set section_title = 'app.contact.delete.title' | trans({'name': contact.name}) %}

{% block body %}

    <h1 class="mx-lg-5">{{ 'app.title' | trans }}</h1>
    <hr class="my-4" />

    <div class="container">
        <h2 class="mb-4">{{ 'app.contact.delete.confirmationTitle' | trans({'%name%': contact.name}) }}</h2>

        {{ form(form) }}
    </div>

{% endblock %}
